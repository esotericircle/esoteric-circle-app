{
  "name": "esoteric-circle-strumenti-di-prova",
  "description": "Strumenti che servono SOLO alle prove, e che il server non deve installare mai. Le funzioni vivono in functions/ e hanno un package.json loro.",
  "private": true,
  "scripts": {
    "test:regole": "npm --prefix functions run test:regole"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0"
  }
}
